module.exports = {
  extends: ['@commitlint/config-conventional'],
  rules: {
    // You can customize rules here if needed
    // For example, to make the scope optional:
    // 'scope-empty': [0, 'never'],
  }
};
